component_id: canvas
package: heatmap
component: HeatMapCanvas
tags:
  - canvas
description: |
  A variation around the [HeatMap](/heatmap) scomponent.
  Well suited  for large data sets as it does not impact DOM tree depth,
  however you'll lose the isomorphic ability and transitions (for now).

  The responsive alternative of this component is `ResponsiveHeatMapCanvas`.
